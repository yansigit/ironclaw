[package]
name = "ironclaw"
version = "1.4.0"
edition = "2024"
rust-version.workspace = true
description = "Secure personal AI assistant that protects your data and expands its capabilities on the fly"
authors = ["NEAR AI <support@near.ai>"]
license = "MIT OR Apache-2.0"
homepage = "https://github.com/nearai/ironclaw"
repository = "https://github.com/nearai/ironclaw"
publish = false

[package.metadata.ironclaw]
layer = "app"

[package.metadata.dist]
dist = true
display-name = "ironclaw"

[package.metadata.wix]
upgrade-guid = "650712A0-A6B4-4584-829A-03A90CE3D7D9"
path-guid = "9BB3F0DB-3513-45E6-A23B-CE5A5E5C1759"
license = false
eula = false

[features]
# Compile in the (off-by-default) mem0 third-party memory provider so an
# `ironclaw-reborn` binary can point the compose-time [memory] binding at a
# self-hosted mem0 OSS server. Mirrors the same feature on `ironclaw_composition`;
# without it a default binary carries no mem0 code or its reqwest/rustls
# transport and a mem0 binding fails closed.
memory-mem0 = [
    "ironclaw_composition/memory-mem0",
]
# Feature bar 4 (dev-only seam): compile the loopback-only OAuth provider
# endpoint constructor used by hermetic standalone E2E tests. Activation also
# requires a debug build plus paired endpoint env vars; release builds fail
# closed if the vars are present.
test-support = [
    "ironclaw_webui/test-support",
]
[dependencies]
anyhow = "1"
async-trait = { version = "0.1" }
chrono = { version = "0.4", features = ["serde"] }
hex = "0.4.3"
clap = { version = "4", features = ["derive", "env"] }
clap_complete = "4.5.0"
# Masked (echo-suppressed) API-key prompt in `onboard`. Same crate v1's setup
# wizard uses for secret-input masking — reused here rather than adding a
# second terminal-masking dependency.
crossterm = "0.29"
dotenvy = "0.15"
ironclaw_host_api = { path = "../../contracts/ironclaw_host_api", version = "0.1.0" }
ironclaw_product_contracts = { path = "../../contracts/ironclaw_product_contracts", version = "0.1.0" }
ironclaw_extension_contracts = { path = "../../contracts/ironclaw_extension_contracts", version = "0.1.0" }
ironclaw_operator = { path = "../../product/ironclaw_operator", version = "0.1.0" }
ironclaw_composition = { path = "../ironclaw_composition", version = "0.1.0" }
ironclaw_config = { path = "../ironclaw_config", version = "0.1.0" }
ironclaw_trace_commons = { path = "../../domains/ironclaw_trace_commons", version = "0.1.0" }
ironclaw_extension_host = { path = "../../extensions/ironclaw_extension_host", version = "0.1.0" }
ironclaw_extension_manager = { path = "../../extensions/ironclaw_extension_manager", version = "0.1.0" }
# DEL-7: the binary is the one generic-side crate allowed to link the concrete
# first-party extensions; it converts the package inventory into composition's
# neutral bundle set and supplies the GSuite/web-access capability registrars.
# Host-runtime types are reached through composition facade re-exports.
# Host API is direct only for neutral operator DTO contracts; auth-owned
# credential visibility policy is named directly from ironclaw_auth after the
# product-auth ownership move.
ironclaw_extension_support = { path = "../../extensions/ironclaw_extension_support", version = "0.1.0" }
ironclaw_auth = { path = "../../domains/ironclaw_auth", version = "0.1.0" }
ironclaw_slack_extension = { path = "../../extensions/packages/slack", version = "0.1.0" }
ironclaw_telegram_extension = { path = "../../extensions/packages/telegram", version = "0.1.0" }
ironclaw_web_app = { path = "../../domains/ironclaw_web_app", version = "0.1.0" }
ironclaw_web_app_extension = { path = "../../extensions/packages/web-app", version = "0.1.0" }
ironclaw_webui = { path = "../../product/ironclaw_webui", version = "0.1.0" }
rand = "0.10"
reqwest = { version = "0.12", default-features = false, features = ["json", "multipart", "rustls-tls-native-roots", "stream"] }
secrecy = "0.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.11"
tempfile = "3"
thiserror = "2"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "signal", "io-util", "io-std", "sync", "time"] }
tokio-util = { version = "0.7", features = ["rt"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }
uuid = { version = "1", features = ["v4", "v5", "serde"] }

[target.'cfg(unix)'.dependencies]
# O_NOFOLLOW for the single-handle, symlink-safe token file open in
# `webui_token::read_token_file_checked` — the flag's raw value differs
# across unix platforms (Linux vs macOS), so it must come from `libc`
# rather than being hardcoded. Already present transitively; pinned here
# as a direct dependency of the code that now uses it.
libc = "0.2"

[dev-dependencies]
# Enable the composition crate's `test-support` seam for CLI unit tests so
# `open_reborn_identity_resolver` (the test-only standalone resolver opener)
# is available when building this crate's tests. Production code uses the
# runtime-owned `RebornRuntime::open_reborn_identity_resolver` accessor
# instead. Feature-unifies with the `[dependencies]` entry above.
ironclaw_composition = { path = "../ironclaw_composition", version = "0.1.0", features = ["test-support"] }
# Canonical workspace-wide process-env test lock. Env-mutating unit tests in
# this crate serialize on `ironclaw_common::env_helpers::lock_env()` — the same
# mutex the composition/llm/auth crates and `src/` use — so no crate defines a
# second, non-serializing lock domain (the #6015 flake). Dev-only: the lock is
# reached solely from `#[cfg(test)]` code in `runtime::test_env`.
ironclaw_common = { path = "../../contracts/ironclaw_common", version = "0.4.2" }
# FaultInjecting decorator: test store fault paths through the real store.
# `commands::onboard::llm_credentials`'s write-first-then-config-ordering test
# builds the real `ironclaw_secrets::SecretStore` over a
# `FaultInjecting<InMemoryBackend>` armed to fail secret writes, so the failing
# put exercises the production store's real error mapping.
ironclaw_filesystem = { path = "../../substrates/ironclaw_filesystem", version = "0.1.0", features = ["test-support"] }
# `commands::onboard::llm_credentials`'s write-ordering test builds the real
# `ironclaw_secrets::SecretStore::ephemeral_over(..)` (plus
# `keychain::generate_master_key_hex`); production onboarding code only ever
# touches secrets through `ironclaw_operator::LlmKeyStore`'s facade
# (`put_plaintext`), so this stays a dev-only dependency — see
# `crates/ironclaw_architecture_tests/tests/reborn_dependency_boundaries.rs::reborn_cli_binary_crate_stays_separate_from_v1_root`.
ironclaw_secrets = { path = "../../substrates/ironclaw_secrets", version = "0.1.0" }
toml = "1.1"

[[bin]]
name = "ironclaw"
path = "src/main.rs"
