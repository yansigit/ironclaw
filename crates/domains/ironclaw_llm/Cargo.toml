[package]
name = "ironclaw_llm"
version = "0.1.0"
edition = "2024"
rust-version.workspace = true
description = "Multi-provider LLM integration with retry, failover, circuit breaker, and response caching"
authors = ["NEAR AI <support@near.ai>"]
license = "MIT OR Apache-2.0"
homepage = "https://github.com/nearai/ironclaw"
repository = "https://github.com/nearai/ironclaw"
publish = false

[package.metadata.ironclaw]
layer = "substrates"

[package.metadata.dist]
dist = false

[features]
bedrock = ["dep:aws-config", "dep:aws-sdk-bedrockruntime", "dep:aws-smithy-types"]
# Opt-in registry-provider factory for composition roots that already own
# provider resolution and do not want v1 SessionManager plumbing.
registry-provider-factory = []
# Opt-in StubLlm + credentials helpers for downstream test code.
test-support = []

[dependencies]
ironclaw_common = { path = "../../contracts/ironclaw_common", version = "0.4.2" }
# Normalized capability identity (`CapabilityId`) for the canonical
# `AgentMessage` tool-call vocabulary — contracts-tier, no execution.
ironclaw_host_api = { path = "../../contracts/ironclaw_host_api", version = "0.1.0" }
ironclaw_safety = { path = "../../substrates/ironclaw_safety", version = "0.2.2" }

async-trait = "0.1"
anyhow = "1"
base64 = "0.23.0"
bytes = "1"
chrono = { version = "0.4", features = ["serde"] }
dirs = "6"
eventsource-stream = "0.2"
flate2 = "1"
futures = "0.3"
hex = "0.4"
open = "5"
rand = "0.10"
urlencoding = "2"
regex = "1"
reqwest = { version = "0.13", default-features = false, features = ["form", "http2", "json", "multipart", "rustls", "stream", "system-proxy"] }
# Rig 0.36 preserves non-streaming OpenAI-compatible `reasoning_content`.
# Older releases classify valid reasoning-only Qwen responses as empty.
rig-core = { version = "0.36", default-features = false, features = ["reqwest", "rustls"] }
rust_decimal = { version = "1", features = ["serde", "serde-with-str", "maths"] }
rust_decimal_macros = "1"
secrecy = { version = "0.10", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.11"
thiserror = "2"
tokio = { version = "1", features = ["sync", "io-util", "io-std", "time", "macros", "rt", "fs", "process", "net"] }
tracing = "0.1"
url = "2"
uuid = { version = "1", features = ["v4", "serde"] }

aws-config             = { version = "1", features = ["behavior-version-latest"], optional = true }
aws-sdk-bedrockruntime = { version = "1", default-features = false, features = ["default-https-client", "rt-tokio", "behavior-version-latest"], optional = true }
aws-smithy-types       = { version = "1", optional = true }

[dev-dependencies]
insta = { version = "1.46.3", features = ["yaml"] }
# Property testing for provider-response parsing (#6524 workstream 9). Chosen
# over a hand-rolled random loop because shrinking is the whole value: a
# failure reports the minimal input that breaks the invariant, not whichever
# 40-character string happened to trip it.
proptest = "1"
tempfile = "3"
tokio = { version = "1", features = ["full", "test-util"] }
tracing-test = "0.2"
